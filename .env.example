# Application under test
BASE_URL=https://practicesoftwaretesting.com
API_BASE_URL=https://api.practicesoftwaretesting.com

# Browser (the engine itself is chosen with pytest --browser)
HEADLESS=true
SLOW_MO=0

# Test behaviour
# When true, UI tests that would submit the contact form intercept the request
# instead of sending it to the demo backend.
MOCK_CONTACT_API=true
